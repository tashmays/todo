<h1> Edit Item </h1>
<%= form_for @item do |f| %>
	<%= f.label :title %>:
	<%= f.text_field :title %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.label :quantity %>
	<%= f.number_field :quantity %>
	
	<%= f.submit %>
<% end %>